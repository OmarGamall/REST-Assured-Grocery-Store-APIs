# Products Test Cases Documentation

This document contains the test cases for the **Products** module of the Simple Grocery Store API, covering both happy path scenarios and boundary/validation checks.

## Products Test Cases Table

| Test ID | Title | Type (Positive/Negative) | Requirement Traceability | Pre-conditions | Test Execution Steps | Expected Result | Risk Level |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_PROD_001** | Verify that `GET /products` returns `200 OK` and only in-stock products when `available` query parameter is set to `true` | Positive | `GET /products`<br>Query Param: `available=true` | Products exist in the inventory with both `inStock=true` and `inStock=false`. | 1. Send a `GET` request to `/products` with `available` query parameter set to `true`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Iterate through each product in the response array and check its `inStock` status. | • HTTP Status: `200 OK`<br>• Response body contains a JSON array of products.<br>• Every product in the array has `"inStock": true`. | **Major** |
| **TC_PROD_002** | Verify that `GET /products` returns `200 OK` and only out-of-stock products when `available` query parameter is set to `false` | Positive | `GET /products`<br>Query Param: `available=false` | Products exist in the inventory with both `inStock=true` and `inStock=false`. | 1. Send a `GET` request to `/products` with `available` query parameter set to `false`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Iterate through each product in the response array and check its `inStock` status. | • HTTP Status: `200 OK`<br>• Response body contains a JSON array of products.<br>• Every product in the array has `"inStock": false`. | **Major** |
| **TC_PROD_003** | Verify that `GET /products` returns `200 OK` and products belonging to the specified category when `category` query parameter is provided | Positive | `GET /products`<br>Query Param: `category` | Products exist in the inventory for all valid categories (`meat-seafood`, `fresh-produce`, `candy`, `bread-bakery`, `dairy`, `eggs`, `coffee`). | 1. Send a `GET` request to `/products` with `category` query parameter set to a valid category (e.g., `coffee`). <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Iterate through each product in the response and verify its category. | • HTTP Status: `200 OK`<br>• Response body contains a JSON array of products.<br>• Every product in the array has its `category` property match the requested category. | **Major** |
| **TC_PROD_004** | Verify that `GET /products` returns `200 OK` and limits the number of products to the value of `results` query parameter when `results` parameter is between 1 and 20 | Positive | `GET /products`<br>Query Param: `results` | At least 5 products exist in the inventory. | 1. Send a `GET` request to `/products` with `results` query parameter set to `5`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Count the number of products in the returned array. | • HTTP Status: `200 OK`<br>• Response body contains a JSON array of products.<br>• The size of the returned array is less than or equal to `5`. | **Major** |
| **TC_PROD_005** | Verify that `GET /products` returns `200 OK` and filters products by category, availability, and limit when `category`, `available`, and `results` query parameters are provided | Positive | `GET /products`<br>Query Params: `category`, `available`, `results` | Products exist in the inventory matching the category and availability filters. | 1. Send a `GET` request to `/products` with query parameters: `category=fresh-produce`, `available=true`, and `results=3`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Verify the size of the returned array and check the attributes of each product. | • HTTP Status: `200 OK`<br>• Response body contains a JSON array of products.<br>• The size of the returned array is less than or equal to `3`.<br>• Every product in the array has `"category": "fresh-produce"` and `"inStock": true`. | **Major** |
| **TC_PROD_006** | Verify that `GET /products` returns `200 OK` and zero products when `results` query parameter is set to `0` | Positive | `GET /products`<br>Query Param: `results=0` | Products exist in the inventory. | 1. Send a `GET` request to `/products` with `results` query parameter set to `0`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Verify the size of the returned array. | • HTTP Status: `200 OK`<br>• Response body contains an empty JSON array `[]`. | **Minor** |
| **TC_PROD_007** | Verify that `GET /products` returns `400 Bad Request` and validation error when an invalid `category` query parameter is provided | Negative | `GET /products`<br>Query Param: `category` | None. | 1. Send a `GET` request to `/products` with `category` query parameter set to `"invalid-category"`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Verify the error message details in the response body. | • HTTP Status: `400 Bad Request`<br>• Response body contains an error object: `{"error": "Invalid value for query parameter 'category'"}`. | **Major** |
| **TC_PROD_008** | Verify that `GET /products` returns `400 Bad Request` and validation error when `results` query parameter is below 0 (e.g., `-1`) | Negative | `GET /products`<br>Query Param: `results` | None. | 1. Send a `GET` request to `/products` with `results` query parameter set to `-1`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Verify the error message details in the response body. | • HTTP Status: `400 Bad Request`<br>• Response body contains an error object: `{"error": "Invalid value for query parameter 'results'"}`. | **Major** |
| **TC_PROD_009** | Verify that `GET /products` returns `400 Bad Request` and validation error when `results` query parameter exceeds 20 (e.g., `1000`) | Negative | `GET /products`<br>Query Param: `results` | None. | 1. Send a `GET` request to `/products` with `results` query parameter set to `1000`. <br>2. Retrieve the response. <br>3. Verify the HTTP status code. <br>4. Verify the error message details in the response body. | • HTTP Status: `400 Bad Request`<br>• Response body contains an error object: `{"error": "Invalid value for query parameter 'results'"}`. | **Major** |
| **TC_PROD_010** | Verify that `GET /products/{productId}` returns `200 OK` and product details when a valid product ID is requested | Positive | `GET /products/:productId`<br>Path Param: `productId` | A valid product exists in the inventory. | 1. Retrieve a valid product from the inventory to obtain its ID. <br>2. Send a `GET` request to `/products/{productId}` using that valid ID. <br>3. Retrieve the response. <br>4. Verify the HTTP status code. <br>5. Deserialize the response and compare the fields (`id`, `category`, `name`, `inStock`) with the expected product details. | • HTTP Status: `200 OK`<br>• Response body contains a single product object matching the requested product ID.<br>• The product object has correct `id`, `category`, `name`, and `inStock` details. | **Critical** |
| **TC_PROD_011** | Verify that `GET /products/{productId}` returns `404 Not Found` and error message when a non-existent product ID is requested | Negative | `GET /products/:productId`<br>Path Param: `productId` | None. | 1. Define a non-existent product ID (e.g., `9999`). <br>2. Send a `GET` request to `/products/9999`. <br>3. Retrieve the response. <br>4. Verify the HTTP status code. <br>5. Verify the error message details in the response body. | • HTTP Status: `404 Not Found`<br>• Response body contains an error object: `{"error": "No product with id 9999"}`. | **Critical** |
